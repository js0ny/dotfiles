# zp Host Status

`zp` is currently idle and not used in daily workflow.

The host config is intentionally kept as a minimal baseline so flake evaluation/checks do not break.
When `zp` is brought back, replace defaults in `default.nix` with real hardware-specific values.
